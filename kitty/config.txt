# begin_kitty_theme
# catppuccin-mocha
include current-theme.conf
# end_kitty_theme

# begin_kitty_fonts
font_size 15.0
font_family      family='meslolgl nerd font mono' postscript_name=meslolglnfm-regular
bold_font        auto
italic_font      auto
bold_italic_font auto
# end_kitty_fonts

# command + d: split vertically in the current directory
# map cmd+d launch --cwd=current --location=vsplit
map kitty_mod+enter new_window_with_cwd

# stop kitty from interfering with show-desktop on my mac
macos_traditional_fullscreen yes

# cursor shape default
cursor_shape block

# cursor style
cursor_blink_interval 0
cursor_stop_blinking_after 0

# thickness tweaks (makes it look modern)
cursor_beam_thickness 1.5
cursor_underline_thickness 2.0

# optional but cool
cursor_trail 1
cursor_trail_decay 0.1 0.3

# when window loses focus
cursor_shape_unfocused hollow

# Add shortcut for quick tab switching
map cmd+1 goto_tab 1
map cmd+2 goto_tab 2
map cmd+3 goto_tab 3
map cmd+4 goto_tab 4
map cmd+5 goto_tab 5
map cmd+6 goto_tab 6
map cmd+7 goto_tab 7
map cmd+8 goto_tab 8
map cmd+9 goto_tab 9

# Browse session files inside a directory and pick one
map f6>p goto_session ~/.config/kitty
