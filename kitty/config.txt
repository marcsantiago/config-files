# --- THEME ---
# begin_kitty_theme
# catppuccin-mocha
include current-theme.conf
# end_kitty_theme

# --- FONTS ---
# begin_kitty_fonts
font_size 15.0
font_family      family='MesloLGL Nerd Font Mono' postscript_name=MesloLGLNFM-Regular
bold_font        auto
italic_font      auto
bold_italic_font auto
# end_kitty_fonts

# --- CURSOR ---
cursor_shape block
cursor_blink_interval 0
cursor_stop_blinking_after 0

# Thickness and Modern UI tweaks
cursor_beam_thickness 1.5
cursor_underline_thickness 2.0
cursor_trail 1
cursor_trail_decay 0.1 0.3

# When window loses focus
cursor_shape_unfocused hollow

# --- MOUSE SETTINGS ---
# Kitty natively passes clicks to Helix. We do not want to override the "grabbed" state.
# (If you ever need to use Kitty's native selection or click a link while in Helix, just hold Shift + Click).

# Disabled to prevent macOS focus desyncs when moving the mouse to switch tabs
focus_follows_mouse no

# Keep this enabled for proper terminal features
shell_integration enabled

# --- KEYBINDINGS ---
# Command + Enter: new window in current directory
map kitty_mod+enter new_window_with_cwd

# Quick tab switching (Cmd + 1-9)
map cmd+1 goto_tab 1
map cmd+2 goto_tab 2
map cmd+3 goto_tab 3
map cmd+4 goto_tab 4
map cmd+5 goto_tab 5
map cmd+6 goto_tab 6
map cmd+7 goto_tab 7
map cmd+8 goto_tab 8
map cmd+9 goto_tab 9

# Browse session files
map f6>p goto_session ~/.config/kitty

# --- MACOS SPECIFIC ---
macos_traditional_fullscreen yes
macos_option_as_alt yes
